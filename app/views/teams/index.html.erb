<!DOCTYPE html>
<html>
<title>W3.CSS Template</title>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
<link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Raleway">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
<style>
#teams {
  font-family: Arial, Helvetica, sans-serif;
  border-collapse: collapse;
  width: 100%;
}

#teams td, #teams th {
  border: 1px solid #ddd;
  padding: 8px;
}

#teams tr:nth-child(even){background-color: #f2f2f2;}

#teams tr:hover {background-color: #ddd;}

#teams th {
  padding-top: 12px;
  padding-bottom: 12px;
  text-align: left;
  background-color: cornflowerblue;
  color: white;
}
</style>
<body>

  <p style="color: green"><%= notice %></p>

  <% @teams = Team.all %>

  <h1><%= t 'teams.index.teams'%></h1>

  <div id="teams">
      <table id="teams">
        <tr>
          <th><%= t 'teams.index.ID'%></th>
          <th><%= t 'teams.index.Name'%></th>
          <th><%= t 'teams.index.Description'%></th>
          <th><%= t 'teams.index.Members'%></th>
          <th><%= t 'teams.index.Team lead'%></th>
          <th><%= t 'users.index.Action' %> </th>
        </tr>
         <% @teams.each do |team| %>
            <tr>
              <td><%=team.id %></td>
              <td><%=team.name %>  </td>
              <td><%=team.description %></td>
              <td>
                    <%y=2%>
                    <%while y<team.members.length %>
                      <%= Employee.find(team.members[y].to_i).name %> 
                      <% y = y+5%>
                      <%if y<team.members.length%>
                        -
                      <%end%>
                    <%end%>
              </td>
              <td><%=team.team_lead %></td>
              <td> <%= button_to "Delete", team, method: :delete %>
                 <%= link_to "Edit", edit_team_path(team) %> </td>
            </tr>
            <tr>
              <td>
              <button class="w3-bar-item w3-button"> <%= link_to I18n.t('button.show'), team %> </button>
              </td>
            </tr>
        <% end %>
      </table>
  </div>
  <br>
  <button class="w3-bar-item w3-button"> <%= link_to I18n.t('button.newteam'), new_team_path %> </button>
  <br>
  <br>
  <a href="http://[::1]:3000/?locale=en" class="w3-bar-item w3-button"><%= t 'button.back' %></a>
</body>
</html>